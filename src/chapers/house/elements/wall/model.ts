export class Wall {
  private length?: number
  private width?: number
  thick?: number
  constructor(length: number, width: number, thick: number) {
    this.length = length
    this.width = width
    this.thick = thick
  }
}

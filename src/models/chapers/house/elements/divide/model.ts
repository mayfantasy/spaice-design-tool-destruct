import { WallDivideBase } from '../base/wall-divide-base'

export class Divide extends WallDivideBase {
  constructor(id: string, length: number, width: number, thick: number) {
    super(id, length, width, thick)
  }
}
